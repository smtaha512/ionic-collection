import { TimeToTitlePipe } from './time-to-title.pipe';

describe('TimeToTitlePipe', () => {
  it('create an instance', () => {
    const pipe = new TimeToTitlePipe();
    expect(pipe).toBeTruthy();
  });
});
